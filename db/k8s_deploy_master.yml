---
- name: 1. 인프라 기초 자동화 (VM - DNS & Mail)
  hosts: dns_mail_vms
  become: yes
  tasks:
    # 앞에서 만든 DNS/Mail 앤시블 태스크들을 여기에 포함
    - name: DNS 서버 설정 실행
      import_tasks: dns_tasks.yml
    - name: 메일 서버 설정 실행
      import_tasks: mail_tasks.yml

- name: 2. 애플리케이션 현대화 (K8s - Web & DB)
  hosts: k8s_master
  tasks:
    - name: DB 파드 배포 (MariaDB)
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: apps/v1
          kind: Deployment
          metadata:
            name: mariadb
          spec:
            # ... (MariaDB 상세 설정) ...

    - name: Web 파드 배포 (FastAPI)
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: apps/v1
          kind: Deployment
          metadata:
            name: fastweb
          spec:
            # ... (Web 상세 설정 및 위에서 만든 DNSConfig 포함) ...
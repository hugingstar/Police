apiVersion: apps/v1
kind: Deployment
metadata:
  name: mariadb-deployment
  namespace: db
  labels:
    app: mariadb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mariadb
  template:
    metadata:
      labels:
        app: mariadb
    spec:
      containers:
      - name: mariadb
        image: mariadb:latest
        env:
        - name: MARIADB_ROOT_PASSWORD
          value: "P@ssw0rd"
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: nfs-db-volume
          mountPath: /var/lib/mysql
      volumes:
      - name: nfs-db-volume
        nfs:
          server: 10.15.0.170
          path: /root/nfs_node/db/ohit/db/sql
---
apiVersion: v1
kind: Service
metadata:
  name: mariadb-service
  namespace: db
spec:
  selector:
    app: mariadb  # Deployment의 template.labels와 일치해야 합니다.
  ports:
    - protocol: TCP
      port: 3306      # 서비스가 노출할 포트
      targetPort: 3306 # 파드 내부 컨테이너 포트
      nodePort: 30306
  type: NodePort
apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-pv-config
  labels:
    service: config
spec:
  capacity:
    storage: 1Gi
  accessModes: ["ReadWriteMany"]
  nfs:
    server: 10.15.0.170
    path: root/nfs_node/config
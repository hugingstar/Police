apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pvc-monitoring
spec:
  storageClassName: ""
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 10Gi
  selector:
    matchLabels:
      service: monitoring